import { FlightListItemDirective } from './flight-list-item.directive';

describe('FlightListItemDirective', () => {
  it('should create an instance', () => {
    const directive = new FlightListItemDirective(null);
    expect(directive).toBeTruthy();
  });
});
